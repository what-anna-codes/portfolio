export interface SubheadingsUI {
  role: string;
  context: string;
  stages: string;
  tools: string;
}

export const subheadings_en: SubheadingsUI = {
  role: "role",
  context: "in context",
  stages: "process stages",
  tools: "tooling"
};

export const subheadings_pl: SubheadingsUI = {
  role: "rola",
  context: "kontekst",
  stages: "fazy procesu",
   tools: "narzędzia"
};
